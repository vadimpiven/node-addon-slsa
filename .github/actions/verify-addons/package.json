{
  "name": "verify-addons",
  "private": true,
  "type": "module",
  "scripts": {
    "type-check": "tsc --noEmit",
    "prebuild": "pnpm run type-check",
    "build": "ncc build index.ts --license licenses.txt --external supports-color",
    "pretest": "pnpm run build",
    "test": "pnpm run type-check",
    "ci-build": "pnpm run build",
    "ci-test": "pnpm run test"
  },
  "devDependencies": {
    "@actions/core": "catalog:",
    "@node-addon-slsa/internal": "workspace:*",
    "@types/node": "catalog:",
    "@vercel/ncc": "catalog:",
    "dedent": "catalog:",
    "typescript": "catalog:",
    "undici": "catalog:"
  }
}
